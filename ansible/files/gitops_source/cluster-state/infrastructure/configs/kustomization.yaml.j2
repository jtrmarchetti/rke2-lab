{{ ansible_managed | comment }}
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Objects that need a controller's CRDs to already exist. Everything here is
  # reconciled only after infra-controllers reports ready — see
  # clusters/dev-lo/infrastructure.yaml for why that ordering is load-bearing.
  - cert-manager
  - longhorn
  - external-secrets
