{{ ansible_managed | comment }}
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # The vendor's own release asset, byte-for-byte. See README.md for the
  # provenance and the checksum that proves it.
  - controller.yaml
