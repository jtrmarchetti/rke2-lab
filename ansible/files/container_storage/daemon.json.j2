{#
  Ansible managed.

  Points the Docker daemon's state at the data volume. Only data-root is set:
  every other setting stays at the package default, so this file does not
  become a second place where daemon behaviour is defined.
#}
{
  "data-root": "{{ container_storage_docker_root }}"
}
