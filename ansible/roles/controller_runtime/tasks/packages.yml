---

- name: packages | Ensure the controller's system packages are installed
  become: true
  ansible.builtin.apt:
    name: "{{ controller_runtime_packages }}"
    state: present
    update_cache: true
    cache_valid_time: 3600
