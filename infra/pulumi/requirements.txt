# Pulumi project dependencies, installed into infra/pulumi/.venv.
#
# Pinned exactly, for the same reason ansible/requirements.yml is: a range
# records what was tested against nothing in particular, and the provider is
# what talks to the hypervisor every VM in the lab lives on.
#
# The controller_runtime role reconciles this environment from this file on
# every run; plan/CONTROLLER.md records the versions as installed.
#
# The proxmoxve provider plugin is not listed here and cannot be. The Pulumi
# CLI downloads it into its own plugin cache on first use, which is the only
# path Pulumi supports.
pulumi==3.259.0
pulumi-proxmoxve==8.4.1
# Host-SSH fallback in modules/pve_cleanup.py (pvesm free of orphan LVs when
# the PVE 9.2 API DELETE /content 501s). ptyprocess is pexpect's dep.
pexpect==4.9.0
